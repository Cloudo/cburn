/// <reference types="vite/client" />
// `import.meta.env.DEV` is vite's, and without this line TypeScript does not know it:
// the development-only readout of what cburn costs itself leans on that flag.
