# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Application data (local, never committed)
*.db
*.db-wal
*.db-shm
cburn.db*

# Node / frontend
node_modules/
web/dist/

# Browser check artefacts
.playwright-mcp/

# macOS / editors
.DS_Store
.idea/
.vscode/

# Working documents in Russian. The repository stays English apart from the
# interface dictionary (web/src/dict.ts), so these live outside git.
.local/

# Local Claude Code settings
.claude/settings.local.json

# Isolated repository copies for parallel work
.claude/worktrees/

# The desktop wrapper build (milestone F): gigabytes of object files.
src-tauri/target/
src-tauri/gen/

# the recordings themselves: the readme carries the encoded mp4, not the 36 MB source
*.mov
